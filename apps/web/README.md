# Web app

The public website and read-only API will live here after the data reliability milestone is complete.

UI implementation intentionally starts after the source registry, normalized item model, exact deduplication, and ingestion health checks are verified.
